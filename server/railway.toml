[build]
builder = "NIXPACKS"
buildCommand = "npm ci"

[deploy]
startCommand = "node --import tsx index.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
