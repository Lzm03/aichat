[build]
builder = "NIXPACKS"
buildCommand = "cd server && npm ci"

[deploy]
startCommand = "cd server && ./node_modules/.bin/tsx index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
